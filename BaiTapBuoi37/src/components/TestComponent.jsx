const TestComponent = ({ num, name }) => {
  //   console.log(param);

  return (
    <>
      <p>
        day la {name} component {num}{" "}
      </p>
      {/* <p>day la test component {param.name}</p> */}
    </>
  );
};

export default TestComponent;
